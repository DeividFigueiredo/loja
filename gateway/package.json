{
  "name": "gateway",
  "version": "1.0.0",
  "description": "Gateway / BFF — API-only (porta 3000). Decide Medusa vs Shopify e normaliza dados para o frontend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "express": "^4.18.0"
  }
}
